import{ArrowRight,Check,Recycle}from'lucide-react';import{Link,useNavigate}from'react-router-dom';import{FormEvent}from'react';import{useDispatch}from'react-redux';import{setRole}from'../../../store';import{Field}from'../../common/UI';export default function Auth({register=false}:{register?:boolean}){const nav=useNavigate(),dispatch=useDispatch();const go=(e:FormEvent)=>{e.preventDefault();dispatch(setRole('resident'));nav('/app/dashboard')};return <div className="auth"><section className="auth-art"><div className="brand light"><span><Recycle/></span><b>earth<i>forward</i></b></div><div><span className="eyebrow">WASTE LESS · LIVE MORE</span><h1>Small actions.<br/>A cleaner tomorrow.</h1><p>Join your community in making waste collection smarter, faster and more rewarding.</p><div className="auth-proof"><span><Check/></span><b>2,480 kg</b> waste diverted this month</div></div></section><section className="auth-form"><form onSubmit={go}><span className="eyebrow">WELCOME TO EARTH FORWARD</span><h1>{register?'Create your account':'Welcome back'}</h1><p>{register?'Start making your neighborhood cleaner today.':'Sign in to keep your neighborhood moving forward.'}</p>{register&&<Field label="Full name"><input required placeholder="Charan Kumar"/></Field>}<Field label="Email or phone"><input required placeholder="you@example.com"/></Field>{register&&<Field label="Phone"><input required placeholder="+91 98765 43210"/></Field>}<Field label="Password"><input required type="password" placeholder="••••••••"/></Field>{register&&<Field label="Confirm password"><input required type="password" placeholder="••••••••"/></Field>}<button className="button wide">{register?'Create account':'Sign in'} <ArrowRight/></button><p className="center">{register?'Already have an account?':'New to Earth Forward?'} <Link to={register?'/auth/login':'/auth/register'}>{register?'Sign in':'Create account'}</Link></p></form></section></div>}
