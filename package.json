{
  "name": "earth-forward",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:push": "npm run db:push --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:bootstrap": "npm run db:bootstrap --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
